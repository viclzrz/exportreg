% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/to_latex.R
\name{to_latex}
\alias{to_latex}
\title{Export a regression table to LaTeX}
\usage{
to_latex(x, file = NULL, format = "fragment", note = NULL, digits = NULL)
}
\arguments{
\item{x}{A \code{regtab_table} object.}

\item{file}{Character or \code{NULL}. If a file path is supplied, the LaTeX
string is written to that file in addition to being printed. Default
\code{NULL}.}

\item{format}{\code{"fragment"} (default) returns only the \code{tabular}
environment. \code{"full"} wraps it in a minimal \code{article} document with
\verb{\\\\usepackage\{booktabs\}}.}

\item{note}{Character or \code{NULL}. Custom text appended after the standard
significance note.}

\item{digits}{Integer or \code{NULL}. Number of decimal places for numeric
formatting. \code{NULL} (default) uses \code{x$digits} set at \code{regtab()} time.
An integer value overrides \code{x$digits} for this render call only.}
}
\value{
Character vector of LaTeX lines, invisibly.
}
\description{
Renders a \code{regtab_table} as a LaTeX tabular environment using booktabs
style. Always prints to the console. Optionally writes to a file.
}
\details{
Stars use LaTeX math-mode notation: \verb{$^\{*\}$}, \verb{$^\{**\}$}, \verb{$^\{***\}$}.
}
\examples{
\dontrun{
m1 <- lm(mpg ~ wt + cyl, data = mtcars)
tab <- regtab(list("(1)" = m1))
to_latex(tab)
to_latex(tab, file = "table1.tex", format = "full")
to_latex(tab, digits = 2L)
}

}
