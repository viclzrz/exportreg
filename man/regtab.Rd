% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regtab.R
\name{regtab}
\alias{regtab}
\title{Build a regression table}
\usage{
regtab(
  models,
  keep = NULL,
  drop = NULL,
  coef_map = NULL,
  factor_labels = NULL,
  add_rows = NULL,
  fe_labels = NULL,
  stars = c(0.1, 0.05, 0.01),
  digits = 3L,
  col_groups = NULL,
  panels = NULL,
  se_format = "se",
  depvar_labels = NULL
)
}
\arguments{
\item{models}{Named list of model objects. Names become column headers.
Supported classes: \code{fixest}, \code{lm}, \code{glm}, \code{felm}, \code{ivreg}.}

\item{keep}{Character vector of regex patterns. Only coefficients whose
raw names match at least one pattern are shown. Applied before \code{drop}.}

\item{drop}{Character vector of regex patterns. Coefficients matching any
pattern are removed. Applied after \code{keep}.}

\item{coef_map}{Named character vector mapping raw coefficient names to
display labels, e.g. \code{c("log_w" = "Log wage")}.}

\item{factor_labels}{Named character vector mapping a variable stem to a
group header label, e.g. \code{c("region" = "Region (ref: Norte)")}.
fixest \code{i()} variables are grouped automatically. Base R factors require
explicit declaration.}

\item{add_rows}{\code{data.frame} of manually supplied rows to append below the
FE block. Must have a \code{label} column plus one column per model name.}

\item{fe_labels}{Named character vector mapping raw FE variable names to
display labels, e.g. \code{c("firm_id" = "Firm FE")}.}

\item{stars}{Numeric vector of p-value thresholds in ascending order.
Default \code{c(0.1, 0.05, 0.01)} produces \code{*}, \verb{**}, \verb{***}.}

\item{digits}{Integer. Number of decimal places. Default \code{3L}.}

\item{col_groups}{Named character vector mapping each model name to a
spanning group header label.}

\item{panels}{Named list of \code{regtab_table} objects for multi-panel tables.
All panels must have identical \code{model_names}. When supplied, \code{models} is
ignored.}

\item{se_format}{Character scalar controlling what appears in the second row
under each coefficient. \code{"se"} (default) shows standard errors in
parentheses; \code{"tstat"} shows absolute t-statistics in brackets; \code{"pvalue"}
shows p-values in brackets.}

\item{depvar_labels}{Named character vector mapping raw extracted dependent
variable strings to display labels,
e.g. \code{c("log(wage)" = "Log Wage")}. \code{NULL} (default) uses the raw
extracted string.}
}
\value{
An object of class \code{regtab_table}.
}
\description{
Takes a named list of model objects and returns a \code{regtab_table} object
that can be exported to Excel or LaTeX via \code{\link[=to_excel]{to_excel()}} and \code{\link[=to_latex]{to_latex()}}.
}
\examples{
\dontrun{
m1 <- lm(mpg ~ wt + cyl, data = mtcars)
m2 <- lm(mpg ~ wt + cyl + hp, data = mtcars)
tab <- regtab(list("(1)" = m1, "(2)" = m2))
print(tab)
}

}
