% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/to_latex.R
\name{tex_coef_row}
\alias{tex_coef_row}
\title{Build a LaTeX coefficient estimate row}
\usage{
tex_coef_row(label, b, p, digits = 3L)
}
\arguments{
\item{label}{Character. Row label (LaTeX-escaped internally).}

\item{b}{Numeric vector. Estimates, one per column; \code{NA} produces an
empty cell.}

\item{p}{Numeric vector. p-values for star assignment, same length as
\code{b}; \code{NA} produces no stars.}

\item{digits}{Integer. Decimal places. Default \code{3L}.}
}
\value{
\code{character(1)} — \code{&}-delimited row ending in \code{ \\}.
}
\description{
Produces one coefficient estimate row: label in column 1, formatted
estimates with math-mode stars in the remaining columns.
}
\examples{
tex_coef_row("Age", b = c(0.123, NA), p = c(0.03, NA))

}
